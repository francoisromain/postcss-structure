{
  "name": "francoisromain.github.io.structure.css",
  "version": "0.1.0",
  "scripts": {
    "build": "postcss --local-plugins -u postcss-import -u postcss-custom-properties -u postcss-structure -u postcss-cssnext -i src/styles.css -o dist/styles.css"
  },
  "dependencies": {
    "structure.css": "^0.5.6"
  },
  "devDependencies": {
    "postcss-cli": "^2.5.1"
  }
}
